apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-customer
  labels:
    app: web-customer
spec:
  replicas: 1
  selector:
    matchLabels:
      name: web-customer
  template:
    metadata:
      labels:
        name: web-customer
    spec:
      containers:
        - name: a2billing
          image: driftwood64/a2billing-docker-web-customer:v1
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: "Rd2MZ9FPugNYrDrvmz8gLF"
            - name: PMA_HOST
              value: "db"
            - name: PMA_PORT
              value: "3306"
            - name: COMPOSER_ALLOW_SUPERUSER
              value: "1"

---
# START Service
apiVersion: v1
kind: Service
metadata:
  name: web-customer
  labels:
    app: web-customer
spec:
  type: LoadBalancer
  ports:
    - port: 8091 #port that the service exposes
      targetPort: 80 #port that the app is receiving requests from via the pod
  selector:
    name: web-customer
# END SERVICE
